probe 
	kernel.function("ext4_file_mmap")
{
	if (@defined($file->f_path->dentry)) {
		ino = $file->f_path->dentry->d_inode->i_ino;
	} else {
		ino = $file->f_dentry->d_inode->i_ino;
	}
	printf("%s,%d,%s,%x,\n", execname(), pid(), probefunc(), ino)
}
